# 🔥 HOTFIX: Discord OAuth Error

## ❌ Проблема:
Ошибка: "Ошибка авторизации через Discord" и 400 Bad Request

## ✅ Решение:

### На сервере выполните:

```bash
# 1. Обновить код
cd /root/TimeWeb
git pull

# 2. Убедиться что login.html обновлен
cd dashboard/public
grep -n "1417959083704582224" login.html

# Должна быть строка с правильным Client ID

# 3. Очистить кэш nginx/браузера
# Перезапустить nginx
systemctl restart nginx

# ИЛИ если используете PM2
pm2 restart all

# 4. В браузере нажать Ctrl+Shift+R (жесткая перезагрузка)
# или Ctrl+F5
```

### Проверка:

Откройте в браузере: `https://bublickrust.ru/login.html`

1. Нажмите F12 (DevTools)
2. Перейдите на вкладку Network
3. Нажмите "Войти через Discord"
4. Проверьте запрос - должен быть `client_id=1417959083704582224`

---

## Если все еще не работает:

Проверьте что в Discord Developer Portal:
- Application → OAuth2 → **Redirects** содержит: `https://bublickrust.ru/signin-discord`
- Нажмите **Save Changes** в Discord Portal

---

## Быстрая диагностика:

```bash
# Проверить что код обновлен
cd /root/TimeWeb
git log --oneline -5

# Должен быть коммит: "Fix Discord OAuth Client ID in login.html"

# Проверить что файл правильный
cd dashboard/public
grep "discordAuthUrl" login.html

# Должно быть: client_id=1417959083704582224
```

