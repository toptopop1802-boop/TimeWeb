# Настройка Discord OAuth для Dashboard

## ✅ Что уже сделано:

1. **Код обновлен** - теперь использует переменные окружения из `.env`
2. **Файл `env-template.txt`** создан с вашим Discord Client Secret
3. **Скрипт `setup-env.sh`** создан для автоматической установки на Linux

## 📋 Инструкция по установке на Linux сервере:

### Шаг 1: Загрузите файлы на сервер

```bash
# Убедитесь что файлы загружены в папку dashboard/
cd /path/to/dsbot/dashboard
ls -la env-production.txt setup-env.sh

# Если env-production.txt нет, используйте env-template.txt
# (потребуется добавить Discord Bot Token вручную)
```

### Шаг 2: Запустите скрипт установки

```bash
# Дайте права на выполнение
chmod +x setup-env.sh

# Запустите скрипт
./setup-env.sh
```

Скрипт автоматически:
- Удалит старый `.env` файл (если есть)
- Создаст новый `.env` из шаблона
- Установит правильные права доступа (600)

### Шаг 3: Проверьте .env файл

```bash
cat .env
```

Должны быть следующие переменные:
```
SUPABASE_URL=https://svlgzmgkodwcufypyeec.supabase.co
SUPABASE_KEY=your_service_role_key_here
DISCORD_BOT_TOKEN=your_bot_token_here
DISCORD_CLIENT_ID=1417959083704582224
DISCORD_CLIENT_SECRET=PkdDkT3vTGcelzMfzXSisYOshvZYEi-y
PORT=3000
HOST=0.0.0.0
PUBLIC_BASE_URL=https://bublickrust.ru
```

### Шаг 4: Заполните оставшиеся ключи

Отредактируйте `.env` и замените:
- `your_service_role_key_here` - на ваш Supabase Service Role Key
- `your_bot_token_here` - на ваш Discord Bot Token

```bash
nano .env
# или
vim .env
```

### Шаг 5: Проверьте настройки в Discord Developer Portal

1. Откройте https://discord.com/developers/applications
2. Выберите ваше приложение
3. Перейдите в **OAuth2** → **General**
4. Проверьте что в **Redirects** есть:
   ```
   https://bublickrust.ru/signin-discord
   ```
5. Если нет - добавьте и нажмите **Save Changes**

### Шаг 6: Перезапустите сервер

```bash
cd /path/to/dsbot
# Остановите текущий процесс (Ctrl+C или kill)

# Запустите снова
npm start
# или
node dashboard/server.js
# или через PM2
pm2 restart dashboard
```

## 🔧 Альтернативный способ (вручную):

Если скрипт не работает, можно создать вручную:

```bash
cd /path/to/dsbot/dashboard

# Удалить старый .env
rm -f .env

# Создать новый из шаблона
cp env-template.txt .env

# Установить права
chmod 600 .env

# Проверить
cat .env
```

## 🐛 Проблемы и решения:

### Ошибка: "invalid_client"
- **Причина:** Client ID или Client Secret неверные
- **Решение:** Проверьте что в `.env` правильные значения

### Ошибка: "redirect_uri_mismatch"
- **Причина:** Redirect URI не добавлен в Discord Portal
- **Решение:** Добавьте `https://bublickrust.ru/signin-discord` в Discord OAuth Redirects

### Кнопка "Save Changes" не появляется в Discord
- **Причина:** Вы ничего не изменили на странице
- **Решение:** Добавьте или измените redirect URI, тогда кнопка появится

## 📝 Важные замечания:

1. **Client Secret уже добавлен** в `env-template.txt`: `PkdDkT3vTGcelzMfzXSisYOshvZYEi-y`
2. **Файл .env не коммитится** в git (он в .gitignore)
3. **Права доступа 600** - только владелец может читать
4. **После изменения .env** нужно перезапустить сервер

## ✅ Проверка работы:

После настройки откройте в браузере:
```
https://bublickrust.ru/login.html
```

Нажмите "Войти через Discord" - должна начаться авторизация через Discord.

Если все настроено правильно:
1. Вас перенаправит на Discord
2. Попросит разрешить доступ
3. Вернет обратно на ваш сайт с авторизацией

---

Готово! 🎉

