# 📘 Инструкция по настройке системы управления Stripe аккаунтами

## 🎯 Что изменилось?

Теперь расширение **НЕ использует хардкод** аккаунтов в коде. Все аккаунты управляются через **веб-интерфейс на вашем сайте** `bublickrust.ru`.

### Преимущества:
✅ Централизованное управление всеми аккаунтами  
✅ Статистика использования (PRO/FREE, дата регистрации, локация)  
✅ Автоматическая ротация (используются редко используемые аккаунты)  
✅ Логирование каждого использования  
✅ Легко добавлять/удалять аккаунты без изменения кода  

## 🚀 Быстрый старт (3 шага)

### Шаг 1: Создать таблицы в базе данных

Откройте **Supabase Dashboard** → SQL Editor и выполните:

```sql
-- Скопируйте весь код из файла dashboard/setup_stripe_accounts.sql
```

Или через терминал:
```bash
cd dashboard
psql -h ваш-хост -U ваш-юзер -d ваша-база -f setup_stripe_accounts.sql
```

### Шаг 2: Добавить аккаунты

#### Вариант А: Через веб-интерфейс (рекомендуется)
1. Откройте https://bublickrust.ru/stripe-accounts.html
2. Вставьте список аккаунтов (каждый на новой строке):
```
hodson00737@bublickrust.ru:Qfwm8yhrkyBB
burton_9495@bublickrust.ru:cuurg!DfCFT1
feofilakt_7693@bublickrust.ru:J6KY!S)N6jWT
```
3. Нажмите "Добавить все аккаунты"

#### Вариант Б: Через Node.js скрипт
```bash
cd dashboard
node add_initial_stripe_accounts.js
```
Этот скрипт автоматически добавит все 17 аккаунтов из вашего списка.

### Шаг 3: Переустановить расширение

1. Откройте `chrome://extensions/`
2. Удалите старую версию
3. Включите "Режим разработчика"
4. "Загрузить распакованное расширение" → выберите папку `Cursor/`

## 📊 Использование веб-интерфейса

### URL: https://bublickrust.ru/stripe-accounts.html

#### Верхняя панель (статистика):
- **Всего аккаунтов** - общее количество
- **PRO аккаунты** - сколько PRO
- **FREE аккаунты** - сколько FREE
- **Активные** - сколько активных (используются расширением)

#### Вкладка "Добавить аккаунты":
- **Массовое добавление** - вставьте список `email:password`
- **Добавить один** - добавить через форму

#### Вкладка "Управление":
- **Фильтры** - по типу (PRO/FREE) и статусу (Активен/Неактивен)
- **Таблица** - все аккаунты с кнопками:
  - 🟢 Активировать / 🔴 Деактивировать
  - 🗑️ Удалить

## 🔧 Как это работает

### 1️⃣ Пользователь открывает Stripe checkout
→ Расширение отправляет запрос на сервер: `GET /api/stripe-accounts/random`

### 2️⃣ Сервер выбирает аккаунт
- Только активные (`is_active = true`)
- Приоритет: давно не использованные (`ORDER BY last_used`)
- Возвращает: `{ email, password, account_type }`

### 3️⃣ Расширение заполняет форму
- Email: из ответа сервера
- Password: из ответа сервера
- Карта: генерируется случайно (алгоритм Луна)

### 4️⃣ После заполнения
→ Расширение логирует: `POST /api/stripe-accounts/log-usage`
```json
{
  "email": "hodson00737@bublickrust.ru",
  "account_type": "PRO",
  "success": true,
  "registration_location": "Moscow, Russia"
}
```

### 5️⃣ Сервер обновляет данные
- Обновляет `last_used` timestamp
- Создает запись в `stripe_accounts_usage_log`
- Если аккаунт был FREE, а стал PRO → обновляет `account_type`

## 📡 API Endpoints

### Для расширения:
```bash
# Получить случайный аккаунт
GET /api/stripe-accounts/random
→ { "email": "...", "password": "...", "account_type": "FREE" }

# Залогировать использование
POST /api/stripe-accounts/log-usage
Body: { "email": "...", "account_type": "PRO", "success": true }
```

### Для управления:
```bash
# Получить все аккаунты
GET /api/stripe-accounts

# Получить статистику
GET /api/stripe-accounts/stats

# Добавить аккаунт
POST /api/stripe-accounts
Body: { "email": "...", "password": "..." }

# Обновить аккаунт
PATCH /api/stripe-accounts/:id
Body: { "is_active": false }

# Удалить аккаунт
DELETE /api/stripe-accounts/:id
```

## 🐛 Что делать если не работает?

### Расширение не получает аккаунты:
1. Откройте DevTools (F12) в браузере на странице Stripe
2. Смотрите вкладку **Console**:
   - ✅ `🌐 Запрашиваем аккаунт Stripe с сервера...`
   - ✅ `✅ Аккаунт получен с сервера: email@...`
   - ❌ `❌ Ошибка получения аккаунта: 404` → нет активных аккаунтов в базе

3. Смотрите вкладку **Network**:
   - Фильтр: `stripe-accounts`
   - Должен быть запрос к `https://bublickrust.ru/api/stripe-accounts/random`
   - Status Code: 200 ✅ или 404 ❌

### Веб-интерфейс не открывается:
```bash
# Проверьте что файл существует:
ls dashboard/public/stripe-accounts.html

# Проверьте что сервер запущен:
curl https://bublickrust.ru/api/stripe-accounts/stats
```

### Аккаунты не добавляются:
```bash
# Проверьте подключение к Supabase:
cd dashboard
node -e "require('dotenv').config(); console.log(process.env.SUPABASE_URL)"

# Должно вывести ваш URL Supabase
```

## 🎨 Пример сценария использования

### День 1: Настройка
```bash
# 1. Создаем таблицы
psql ... -f setup_stripe_accounts.sql

# 2. Добавляем аккаунты
node add_initial_stripe_accounts.js

# 3. Проверяем
curl https://bublickrust.ru/api/stripe-accounts/stats
# → {"total":17,"pro":0,"free":17,"active":17}
```

### День 2-30: Работа
- Пользователи используют расширение
- Аккаунты автоматически ротируются
- Статистика накапливается

### Раз в неделю: Мониторинг
1. Открываете https://bublickrust.ru/stripe-accounts.html
2. Смотрите статистику:
   - Сколько PRO аккаунтов создано
   - Когда последний раз использовался каждый аккаунт
3. Добавляете новые аккаунты если нужно

## 📝 Файлы в проекте

### Backend (сервер):
- `dashboard/setup_stripe_accounts.sql` - SQL для создания таблиц
- `dashboard/server.js` - добавлены API endpoints (строки 2476-2747)
- `dashboard/add_initial_stripe_accounts.js` - скрипт для добавления аккаунтов

### Frontend (веб-интерфейс):
- `dashboard/public/stripe-accounts.html` - страница управления аккаунтами

### Extension (расширение):
- `Cursor/stripe-content.js` - обновлен для работы с API (строки 12-67)
- `Cursor/manifest.json` - добавлены разрешения для API
- `Cursor/SETUP_STRIPE_ACCOUNTS.md` - подробная документация (English)
- `Cursor/ИНСТРУКЦИЯ.md` - этот файл (Русский)

## 🎉 Готово!

Теперь у вас полноценная система управления аккаунтами:
- ✅ Расширение получает данные с сервера
- ✅ Веб-интерфейс для управления
- ✅ Статистика и логи использования
- ✅ Автоматическая ротация аккаунтов

**Вопросы?** Смотрите подробную документацию в `SETUP_STRIPE_ACCOUNTS.md`

