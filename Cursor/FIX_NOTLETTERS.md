# 🔧 Исправление ошибок NotLetters API

## Ошибка: 401 Unauthorized

Эта ошибка означает что токен или пароль NotLetters неверны.

### Решение:

#### 1. Получите новый API токен NotLetters

1. Откройте https://notletters.com/account
2. Войдите в свой аккаунт
3. Найдите раздел "API Token" или "Настройки API"
4. Скопируйте токен

#### 2. Обновите токен в расширении

Откройте файл `Cursor/background.js` и найдите строку ~87:

```javascript
const NOTLETTERS_TOKEN = 'y0iRqPnAEihzo2qdHV9YPFwLv6CASSHJ';
```

Замените на ваш новый токен:

```javascript
const NOTLETTERS_TOKEN = 'ваш_новый_токен_здесь';
```

#### 3. Проверьте пароль аккаунта

В том же файле найдите строку ~81:

```javascript
const NOTLETTERS_ACCOUNTS = [
  { email: 'andrews197937@bublickrust.ru', password: 'oyc1YAfSzrw4' }
];
```

Убедитесь что:
- Email правильный
- Пароль актуальный

Если пароль изменился, обновите его:

```javascript
const NOTLETTERS_ACCOUNTS = [
  { email: 'ваш_email@домен.com', password: 'ваш_новый_пароль' }
];
```

#### 4. Переустановите расширение

1. Откройте `chrome://extensions/`
2. Нажмите кнопку "Обновить" (круговая стрелка) на вашем расширении
3. Или удалите и установите заново

---

## Ошибка: 523 Origin is unreachable

Эта ошибка означает что сервис NotLetters временно недоступен.

### Решение:

1. **Подождите** - сервис может быть временно недоступен
2. **Проверьте статус** - откройте https://notletters.com в браузере
3. **Используйте альтернативу** - пока NotLetters недоступен, расширение будет использовать случайно сгенерированные email

### Фолбэк

Расширение автоматически переключится на генерацию случайных email если NotLetters недоступен.

---

## Ошибка: Extension context invalidated

Это означает что расширение было перезагружено во время работы.

### Решение:

1. **Обновите страницу** - нажмите F5 или Ctrl+R
2. **Не обновляйте расширение** во время его работы
3. Если проблема повторяется - перезапустите браузер

---

## Другие проблемы

### OTP поля не найдены

Если видите ошибку `OTP поля не найдены или их недостаточно`:

1. Дождитесь полной загрузки страницы
2. Проверьте что вы на правильной странице (email-verification)
3. Обновите страницу и попробуйте снова

### Проблемы с очисткой cookies

Если видите `No host permissions for cookies`:

1. Откройте `chrome://extensions/`
2. Найдите ваше расширение
3. Нажмите "Подробнее"
4. Проверьте что разрешения включены:
   - `https://cursor.com/*`
   - `https://authenticator.cursor.sh/*`

---

## Отладка

### Включите подробные логи

1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Введите: `localStorage.setItem('debug', 'true')`
4. Перезагрузите страницу

### Проверьте логи расширения

1. Откройте `chrome://extensions/`
2. Найдите ваше расширение
3. Нажмите "Service Worker" или "background page"
4. Смотрите логи в консоли

### Тестовый запрос к NotLetters API

Откройте консоль в DevTools и выполните:

```javascript
fetch('https://api.notletters.com/v1/letters', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer ваш_токен'
  },
  body: JSON.stringify({
    email: 'ваш_email@домен.com',
    password: 'ваш_пароль',
    filters: { search: 'test' }
  })
})
.then(r => r.json())
.then(d => console.log('SUCCESS:', d))
.catch(e => console.error('ERROR:', e));
```

Если видите `401` - токен/пароль неверны.

---

## Быстрое решение всех проблем

```bash
# 1. Обновите background.js с правильными данными
# 2. Переустановите расширение
cd Cursor
# 3. Откройте chrome://extensions/
# 4. Нажмите "Обновить"
```

---

**Если проблемы остались** - создайте issue на GitHub с полными логами из консоли.

